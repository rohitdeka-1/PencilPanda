<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        * {
            box-sizing: border-box;
        }
        body {
            margin: 0;
            padding: 0;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            background-color: #0a0a0a;
            color: white;
            width: 100%;
        }
        .container {
            max-width: 600px;
            width: 100%;
            margin: 0 auto;
            background-color: #000000;
        }
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            padding: 30px 15px;
            text-align: center;
        }
        .logo {
            width: 60px;
            height: 60px;
            margin-bottom: 10px;
        }
        .header h1 {
            color: #ffffff;
            margin: 0;
            font-size: 22px;
            font-weight: 700;
        }
        .content {
            padding: 30px 15px;
        }
        .success-icon {
            text-align: center;
            margin-bottom: 20px;
        }
        .success-badge {
            display: inline-block;
            background: linear-gradient(135deg, #10b981 0%, #059669 100%);
            color: white;
            padding: 10px 20px;
            border-radius: 50px;
            font-weight: 600;
            font-size: 14px;
        }
        .title {
            font-size: 24px;
            font-weight: bold;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            text-align: center;
            margin-bottom: 10px;
            word-wrap: break-word;
        }
        .subtitle {
            font-size: 14px;
            color: #9ca3af;
            text-align: center;
            margin-bottom: 25px;
            word-wrap: break-word;
        }
        .order-box {
            background: linear-gradient(135deg, rgba(102, 126, 234, 0.1) 0%, rgba(118, 75, 162, 0.1) 100%);
            border: 1px solid rgba(102, 126, 234, 0.3);
            border-radius: 12px;
            padding: 20px 15px;
            margin-bottom: 25px;
        }
        .order-row {
            display: flex;
            justify-content: space-between;
            padding: 10px 0;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            gap: 10px;
        }
        .order-row:last-child {
            border-bottom: none;
        }
        .order-label {
            color: #9ca3af;
            font-size: 13px;
            flex-shrink: 0;
        }
        .order-value {
            color: #ffffff;
            font-weight: 600;
            font-size: 13px;
            text-align: right;
            word-break: break-all;
        }
        .section-title {
            font-size: 18px;
            font-weight: bold;
            color: #ffffff;
            margin-bottom: 15px;
            padding-bottom: 10px;
            border-bottom: 2px solid rgba(102, 126, 234, 0.3);
        }
        .product-item {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.05);
            border-radius: 10px;
            padding: 15px;
            margin-bottom: 12px;
        }
        .product-name {
            font-size: 16px;
            font-weight: 600;
            color: #ffffff;
            margin-bottom: 8px;
            word-wrap: break-word;
        }
        .product-details {
            font-size: 13px;
            color: #9ca3af;
            margin-bottom: 5px;
        }
        .product-price {
            font-size: 18px;
            font-weight: 700;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-top: 8px;
        }
        .total-section {
            background: linear-gradient(135deg, rgba(102, 126, 234, 0.15) 0%, rgba(118, 75, 162, 0.15) 100%);
            border: 1px solid rgba(102, 126, 234, 0.3);
            border-radius: 12px;
            padding: 20px 15px;
            margin-top: 25px;
        }
        .total-row {
            display: flex;
            justify-content: space-between;
            padding: 8px 0;
            font-size: 14px;
            color: #d1d5db;
        }
        .total-row.final {
            border-top: 2px solid rgba(102, 126, 234, 0.5);
            margin-top: 12px;
            padding-top: 15px;
            font-size: 20px;
            font-weight: bold;
            color: #ffffff;
        }
        .button {
            display: inline-block;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: #ffffff;
            text-decoration: none;
            padding: 14px 30px;
            border-radius: 10px;
            font-weight: 600;
            margin: 15px 0;
            text-align: center;
            font-size: 14px;
        }
        .info-box {
            background: rgba(16, 185, 129, 0.1);
            border: 1px solid rgba(16, 185, 129, 0.3);
            border-radius: 10px;
            padding: 15px;
            margin: 20px 0;
        }
        .info-box p {
            color: #10b981;
            margin: 0;
            font-size: 13px;
            line-height: 1.6;
        }
        .footer {
            background-color: rgba(255, 255, 255, 0.02);
            padding: 20px 15px;
            text-align: center;
            border-top: 1px solid rgba(255, 255, 255, 0.05);
        }
        .footer-text {
            font-size: 13px;
            color: #6b7280;
            margin: 6px 0;
            word-wrap: break-word;
        }
        .footer a {
            color: #667eea;
            text-decoration: none;
            word-break: break-all;
        }
        
        @media only screen and (max-width: 600px) {
            .title {
                font-size: 20px;
            }
            .total-row.final {
                font-size: 18px;
            }
            .header h1 {
                font-size: 20px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <img src="cid:logo" alt="OTTMOMO Logo" class="logo">
            <h1>OTTMOMO</h1>
        </div>

        <!-- Content -->
        <div class="content">
            <!-- Success Badge -->
            <div class="success-icon">
                <div class="success-badge">✓ Payment Successful</div>
            </div>

            <!-- Title -->
            <h2 class="title">Order Placed Successfully!</h2>
            <p class="subtitle">Thank you for your purchase, {{customerName}}!</p>

            <!-- Order Information -->
            <div class="order-box">
                <div class="order-row">
                    <span class="order-label">Order ID</span>
                    <span class="order-value">#{{orderId}}</span>
                </div>
                <div class="order-row">
                    <span class="order-label">Transaction ID</span>
                    <span class="order-value">{{transactionId}}</span>
                </div>
                <div class="order-row">
                    <span class="order-label">Order Date</span>
                    <span class="order-value">{{orderDate}}</span>
                </div>
                <div class="order-row">
                    <span class="order-label">Payment Method</span>
                    <span class="order-value">XtraGate</span>
                </div>
            </div>

            <!-- Products -->
            <h3 class="section-title">📦 Order Items</h3>
            <div class="products-list">
                {{#each products}}
                <div class="product-item">
                    <div class="product-name">{{this.name}}</div>
                    <div class="product-details">⏱️ Duration: {{this.duration}}</div>
                    <div class="product-details">📱 Quantity: {{this.quantity}}</div>
                    <div class="product-price">₹{{this.price}}</div>
                </div>
                {{/each}}
            </div>

            <!-- Total -->
            <div class="total-section">
                <div class="total-row final">
                    <span>Total Amount Paid</span>
                    <span>₹{{totalAmount}}</span>
                </div>
            </div>

            <!-- Info Box -->
            <div class="info-box">
                <p>
                    <strong>✨ What's Next?</strong><br>
                    Your subscription details will be sent to you within 24 hours via email. 
                    You can also view your order details anytime by clicking the button below.
                </p>
            </div>

            <!-- View Order Button -->
            <center>
                <a href="{{frontendUrl}}/orders" class="button">View My Orders</a>
            </center>
        </div>

        <!-- Footer -->
        <div class="footer">
            <p class="footer-text">Thank you for choosing OTTMOMO! 🎉</p>
            <p class="footer-text">Questions? Contact us at <a href="mailto:support@OTTMOMO.com">support@OTTMOMO.com</a></p>
            <p class="footer-text" style="margin-top: 20px; font-size: 12px; color: #4b5563;">
                © {{year}} OTTMOMO. All rights reserved.
            </p>
        </div>
    </div>
</body>
</html>
