<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background-color: #0a0a0a;
            color: white;
        }
        .container {
            max-width: 600px;
            margin: 20px auto;
            background-color: #000000;
            border: 1px solid rgba(102, 126, 234, 0.3);
            border-radius: 12px;
        }
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            padding: 30px 20px;
            text-align: center;
            border-radius: 12px 12px 0 0;
        }
        .header h1 {
            color: #ffffff;
            margin: 0;
            font-size: 24px;
            font-weight: 700;
        }
        .alert-badge {
            display: inline-block;
            background: #10b981;
            color: white;
            padding: 8px 20px;
            border-radius: 50px;
            font-weight: 600;
            font-size: 14px;
            margin-bottom: 10px;
        }
        .content {
            padding: 30px;
        }
        .info-box {
            background: rgba(102, 126, 234, 0.1);
            border: 1px solid rgba(102, 126, 234, 0.3);
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 20px;
        }
        .info-row {
            display: flex;
            justify-content: space-between;
            padding: 10px 0;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
        .info-row:last-child {
            border-bottom: none;
        }
        .label {
            color: #9ca3af;
            font-size: 14px;
        }
        .value {
            color: #ffffff;
            font-weight: 600;
            font-size: 14px;
            text-align: right;
        }
        .amount {
            font-size: 28px;
            font-weight: bold;
            background: linear-gradient(135deg, #10b981 0%, #059669 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            text-align: center;
            margin: 20px 0;
        }
        .footer {
            background-color: rgba(255, 255, 255, 0.02);
            padding: 20px;
            text-align: center;
            border-radius: 0 0 12px 12px;
            font-size: 12px;
            color: #6b7280;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="alert-badge">🔔 New Order Alert</div>
            <h1>Payment Received!</h1>
        </div>
        
        <div class="content">
            <p style="color: #d1d5db; font-size: 16px; text-align: center; margin-bottom: 25px;">
                A customer has successfully completed a payment.
            </p>

            <div class="info-box">
                <div class="info-row">
                    <span class="label">Customer Name</span>
                    <span class="value">{{customerName}}</span>
                </div>
                <div class="info-row">
                    <span class="label">Customer Email</span>
                    <span class="value">{{customerEmail}}</span>
                </div>
                {{#if customerWhatsapp}}
                <div class="info-row">
                    <span class="label">WhatsApp</span>
                    <span class="value">+91 {{customerWhatsapp}}</span>
                </div>
                {{/if}}
                <div class="info-row">
                    <span class="label">Order ID</span>
                    <span class="value">#{{orderId}}</span>
                </div>
                <div class="info-row">
                    <span class="label">Transaction ID</span>
                    <span class="value">{{transactionId}}</span>
                </div>
                <div class="info-row">
                    <span class="label">Order Date</span>
                    <span class="value">{{orderDate}}</span>
                </div>
            </div>

            <div class="amount">₹{{totalAmount}}</div>

            <p style="color: #9ca3af; font-size: 14px; text-align: center;">
                Login to the admin panel to view complete order details.
            </p>
        </div>

        <div class="footer">
            © {{year}} OTTMOMO Admin Notification
        </div>
    </div>
</body>
</html>
